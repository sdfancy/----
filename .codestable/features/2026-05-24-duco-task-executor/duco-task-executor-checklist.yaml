feature: 2026-05-24-duco-task-executor
created: 2026-05-24

steps:
  - action: "结果语义收紧：让 DequeueCoordinator 区分 taskFinished(ok)，失败不发 XD"
    exit_signal: "新增失败任务测试证明 ok=false 不 mark done、不反馈 XD。"
    status: done
  - action: "payload 与 motion 名词：补 ArmPayload、MotionSegment、MotionRecipe、PlannedRobotTask"
    exit_signal: "默认 payload、合法 payload、非法 payload 都有解析/校验结果。"
    status: done
  - action: "planner 计算节点：实现 raw payload + recipe 到 segment 序列的转换"
    exit_signal: "测试可验证 movej_pose2 -> IO on -> movel -> IO off 规划顺序。"
    status: done
  - action: "DUCO seam 扩展：为 motion/IO 增加 client 接口和 recording fake"
    exit_signal: "无 SDK 构建通过，测试能记录 motion/IO 调用。"
    status: done
  - action: "controller 执行编排：DUCO controller 用 per-arm worker 串行执行 planned task"
    exit_signal: "默认空任务、有效任务、规划失败、DUCO -1 均有信号路径。"
    status: done
  - action: "回归守护：复跑 PLC、相机、fake robot、DUCO controller 测试"
    exit_signal: "spray_tests.exe 通过，默认构建不依赖现场 SDK。"
    status: done

checks:
  - item: "core::RobotTask 保留 raw payload，DUCO 运动细节不暴露到 core"
    source: 名词契约
    status: passed
  - item: "新增 ArmPayload、MotionSegment、MotionRecipe、PlannedRobotTask，并覆盖默认、合法、非法 payload"
    source: 名词契约
    status: passed
  - item: "有效 payload + recipe 生成 movej_pose2 -> IO on -> movel -> IO off 序列"
    source: 验收场景
    status: passed
  - item: "IDucoClient 暴露 motion/IO seam，默认无 SDK 构建仍通过"
    source: 挂载点
    status: passed
  - item: "DucoRobotController 对非默认任务走 planner 和 per-arm worker"
    source: 编排骨架
    status: passed
  - item: "taskFinished(ok=false) 不 mark done、不发送正常 XD"
    source: 流程级约束
    status: passed
  - item: "默认空任务不调用 DUCO motion/IO，仍反馈 XN/XD"
    source: 验收场景
    status: passed
  - item: "payload 无效、recipe 缺失、DUCO 返回 -1 时 warning 且不误发 XD"
    source: 验收场景
    status: passed
  - item: "stop/pause/resume/readStatus 继续使用 control/status client，不混用 motion client"
    source: 验收场景
    status: passed
  - item: "不修改 PLC 9999/9090 或相机 legacy/dual camera 外部报文"
    source: 范围守护
    status: passed
  - item: "不恢复旧机械臂 data/1 或 1,count TCP 协议"
    source: 范围守护
    status: passed
  - item: "不新增 Qt HMI、Modbus 客户端或 recipe UI"
    source: 范围守护
    status: passed
